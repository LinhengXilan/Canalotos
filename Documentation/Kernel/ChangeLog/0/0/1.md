Kernel/ChangeLog/0/0/1.md

---

## commit: 
## 8: 2026-2-16

### M: Lib::printf现在返回uint8_t类型的使用的缓冲区长度

#### 文件

	Include/Lib/Print.h
	Lib/Print.cpp

#### 描述

	void printf(char* buffer, const char* string, ...);
	-> uint8_t printf(char* buffer, const char* string, ...);

### M: 重命名CString为String

#### 文件

	Include/Lib/String.h
	Lib/String.cpp

#### 描述

	Include/Lib/CString.h -> Include/Lib/String.h
	Lib/CString.cpp -> Lib/String.cpp

### M: 将Shell.h中的模板定义移至Shell.inl

#### 文件

	Include/Shell/Shell.h
	Include/Shell/Shell.inl

#### 描述

	将
	template<typename... Args>
	public void Shell::Print(const char* string, Args... args)
	的定义移至Shell.inl

### A: Shell::Print支持转义字符

#### 文件

	Include/Shell/Shell.h
	Shell/Shell.cpp

#### 描述

	向类Shell中添加成员
	```
	uint8_t m_CursorX = 0;
	uint8_t m_CursorY = 0;
	```
	用于记录当前光标位置
	并在`private void Shell::Write()`中处理转义字符

### A: BootLoader向Kernel传递内存信息

#### 文件

	Include/Boot.h
	Kernel/Kernel.cpp

#### 描述

	内存信息结构如下
	```
	struct EFIMemoryMap
	{
		uint32_t Type;
		uint32_t Reserved0;
		address_t PhysicalStart;
		address_t VirtualStart;
		uint64_t NumberOfPages;
		uint64_t Attribute;
		uint64_t Reserved1;
	};
	
	struct EFIDataMemory
	{
		uint64_t Size;
		EFIMemoryMap* MemoryMap;
		uint64_t MapKey;
		uint64_t DescriptorSize;
		uint32_t DescriptorVersion;
	};
	```
	并打印前一百项