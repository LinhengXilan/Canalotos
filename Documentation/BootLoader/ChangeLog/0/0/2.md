BootLoader/ChangeLog/0/0/2.md

---

## commit: 409b9cf283e447c31bfb356a3f17c0e55da48274
## 12: 2026-2-16

### M: 默认为RELEASE

#### 文件

	Source/Config.h

#### 描述

	不再默认定义DEBUG宏

### A: 获取内存信息

#### 文件

	Source/KernelData.h
	Source/Memory.h
	Source/Memory.c
	Source/UefiMain.c

#### 描述

	在Source/KernelData.h中定义了内核将要使用的内存信息结构体:
	typedef struct
	{
		UINTN Size;
		EFI_MEMORY_DESCRIPTOR* Buffer;
		UINTN MapKey;
		UINTN DescriptorSize;
		UINT32 DescriptorVersion;
	}EFI_DATA_MEMORY;
	
	在Source/Memory.h和Source/Memory.c定义了
	EFI_STATUS GetEFIDataMemory(EFI_SYSTEM_TABLE* systemTable, EFI_DATA_MEMORY* memory);
	函数用于获取内存星信息

### A: 退出UEFI启动服务

#### 文件

	Source/UefiMain.c

#### 描述

	调用
	EFI_STATUS EFIAPI EFI_BOOT_SERVICES::ExitBootServices(IN EFI_HANDLE ImageHandle, IN UINTN MapKey);
	来退出UEFI启动服务

---

## commit: 5b5e5db4e6011140446c17de5ed773d43c1244e9
## 13: 2026-2-28

### M: 将edk2更新为edk2-stable202602

### F: 修复编译问题

#### 文件

	BootLoader.dsc
	BootLoader.inf
	Makefile

#### 描述

	现在编译目标文件为Bootx64.efi

---

## commit: 
## 14: 2026-3-20

### A: 将错误返回代码编写为宏

#### 文件

	Error.h
	File.c
	Graphics.c
	Init.c
	Memory.c
	UefiMain.c

#### 描述

	宏定义位于Error.h中
	#define CANALOTOS_ERROR(x) do { if (EFI_ERROR((x))) { return (x); } } while (0)
	
	#ifdef CANALOTOS_DEBUG
	#define CANALOTOS_ERROR_MESSAGE(x, msg1, msg2, ...) do { if (EFI_ERROR((x))) { Print(L##msg1); Print(L" ERROR %d: ", (x) Print(L##msg2, ##__VA_ARGS__); Print(L"\n"); return (x); } } while(0)
	#else
	#define CANALOTOS_ERROR_MESSAGE(x, msg1, msg2, ...) do { if (EFI_ERROR((x))) { return (x); } } while (0)
	#endif

	现在统一使用此文件中的宏进行错误返回
